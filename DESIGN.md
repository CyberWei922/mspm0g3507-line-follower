# mspm0g3507_lineTracker 设计说明

版本：`2.0`

这是替代旧版单文件固件的模块化工程。2.0 已完成源码、模块边界、编译和
当前车辆实测验证；旧版仍由 Git 历史和 1.x 标签保留。

## 控制结构

```text
自检 -> 状态0停车
          ├─ K1 + 正常起始线 -> 状态1直线循迹
          └─ K1 + 全白起点   -> 状态2陀螺仪自主直行

状态1 --持续较大Yaw角速度--> 状态3普通弯道循迹
状态3 --角速度稳定且直线形态持续--> 状态1
状态1/3 --可信左直角--> 状态4
状态1/3 --可信右直角--> 状态5
状态4/5 --连续捕获新线--> 状态1
状态2 --捕获有效直线--> 状态1
任意运行状态 --丢线、通信、转角安全限制--> 状态0
```

### 状态0：停车/待启动

- 上电自检无论结果如何都先停车。
- OLED显示原始灰度、模块状态和故障码；OLED失败不阻止核心运行。
- K1读取一次当前灰度：连续有效直线进入状态1，全白进入状态2，离散/全黑/过宽形态报错并保持状态0。
- K4在运行时立即停车并回到状态0。

### 状态1：直线循迹

- 使用八路数字灰度的加权位置误差 `-7～+7`。
- 控制器是 `P+D`，初始 `Kp=18、Kd=8、Ki=0`。
- 基础速度随最外侧黑线探头逐级下降：200、160、135、110。
- 普通循迹车轮只减速到0，不反转，避免布料上卡顿。
- 进入状态1后的前600 ms关闭弯道门控，待直线形态稳定后才允许进入状态3。
- 直角检测优先级高于普通弯道切换。

### 状态2：自主直行

- 只用于起步时完全没有黑线的情况。
- 记录进入状态时的相对航向，以MPU6050 Z轴角度误差做低增益方向保持。
- 只允许“捕获稳定有效直线后进入状态1”或“30 s超时回状态0”。

### 状态3：普通弯道/ S弯

- 仍以灰度位置为主，不强行把车辆拉回某个绝对Yaw角。
- 使用更积极的 `Kp=20、Kd=6`，中心基础速度160。
- MPU6050的滤波角速度只做较弱阻尼，避免和主动转弯相互对抗。
- 角速度瞬间过零不能退出；必须同时满足低角速度和直线灰度持续240 ms。

### 状态4/5：左/右直角弯

- 识别可信的中心+外侧组合后立即进入对应方向。
- 分为“离开旧中心线”和“重新捕获新线”两阶段，避免把触发时的旧线误认为新线。
- 移植稳定参考工程的定速原地转弯：速度125，内侧轮反转、外侧轮正转。
- 左弯输出为左轮-125、右轮+125；右弯完全镜像。
- 离开旧中心线后，不再要求固定55°或 `straight_line`；新线进入中心且原触发侧清空，连续两个20 ms周期即退出。
- 最短转动时间100 ms，防止将旧线误认为新线；全白由直角弯总超时统一保护。
- 最大累计转角360°、总超时12 s，均为异常安全上限，不是正常目标角度。

## 陀螺仪处理

- MPU6050地址0x68，元件面朝上、车头朝照片左侧时使用Z轴。
- 上电先等待2 s，再采样400次计算Z轴零偏。
- 配置±250 °/s、约42 Hz DLPF、200 Hz内部采样。
- 原始角速度先经过三点中值，再进入定点标量卡尔曼滤波。
- 角度用定点毫度累计，主循环不依赖浮点运算。
- 只在静止超过500 ms且角度在±6°内时，缓慢将角度向0衰减。
- 角度比例系数集中在 `app_config.h`，目前为1.000，后续可做原地360°标定。

## 模块边界

- `BSP/Eight_Tracking`：PA14 OUT、PA15/16/17选通和灰度特征。
- `BSP/MPU6050`：PA1/PA0软件I²C、寄存器、零偏、卡尔曼和角度。
- `BSP/Motor`：PA12/PA13软件I²C、电机板0x26/0x06协议和M1～M4符号。
- `BSP/OLED`：PA31/PA28硬件I²C0，调试输出，不决定运动。
- `BSP/Key`、`BSP/Buzzer`、`BSP/LED`、`BSP/System`：输入、反馈和1 ms节拍。
- `APP`：状态机、循迹PD和直角转弯策略。
- 根目录只保留 `main.c`、配置、说明和SysConfig。

## 当前已知的硬件限制

灰度模块是GPIO多路选通，没有I²C应答，软件无法绝对区分“模块断开”和
“输出被硬件拉成固定电平”。启动时由灰度图案合法性判断进入循迹、自主直行
或故障状态，实车调试仍需在OLED上核对八路RAW数据。

第一次实车验证必须先架空车轮，只测试电机方向、传感器位序和状态切换。
