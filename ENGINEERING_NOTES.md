# 2026 电赛小车：算法决策、实测引脚与避坑记录

更新日期：2026-07-25

这份文件用于保存外部高手建议、已经在实车上验证的结论、尚待验证的算法假设，以及开发过程中踩过的坑。后续修改代码前先检查本文件，避免重复犯错。

状态标记：

- **已验证**：已经由当前实物、编译结果或官方资料确认。
- **计划采用**：下一版准备实现，但尚未完成实车验证。
- **待验证**：可以试验，不能直接当成事实。
- **不要采用**：已发现错误或风险较高。

## 1. 当前硬件与接口

### 1.1 已验证接口

| 功能 | MSPM0G3507 引脚 | 说明 |
| --- | --- | --- |
| 电机驱动 SCL | PA12 | 软件 I2C |
| 电机驱动 SDA | PA13 | 软件 I2C |
| 八路灰度 OUT | PA14 | 数字输出 |
| 八路灰度 AD0 | PA15 | 通道选择 |
| 八路灰度 AD1 | PA16 | 通道选择 |
| 八路灰度 AD2 | PA17 | 通道选择 |
| MPU6050 SDA | PA0 | 软件 I2C，AD0 接 GND，地址 0x68 |
| MPU6050 SCL | PA1 | 软件 I2C |
| 扩展板蜂鸣器 | PB24 | TIMA0 CCP3，约 3.1 kHz PWM |
| K1 | PA2 | 按下为低电平 |
| K2 | PB19 | 按下为低电平 |
| K3 | PB20 | 按下为低电平 |
| K4 | PA23 | 按下为低电平 |
| DAPLink SWDIO | PA19 | 核心板背面焊盘 |
| DAPLink SWCLK | PA20 | 核心板背面焊盘 |
| DAPLink RST | RST | 推荐接入，便于复位下连接 |

### 1.2 已验证电机顺序

| 驱动通道 | 车轮 | 前进命令符号 |
| --- | --- | --- |
| M1 | 左前 | 正 |
| M2 | 右前 | 负 |
| M3 | 左后 | 负 |
| M4 | 右后 | 正 |

左右差速必须按左右车轮分组：左侧为 M1、M3，右侧为 M2、M4。不能按前后轮分组。

## 2. MPU6050 高手建议记录

来源：用户提供的实战建议。

### 2.1 上电零偏校准

原建议：

1. 上电后等待 2 秒，让 MPU6050 自热并趋于稳定。
2. 保持整车完全静止。
3. 连续采样 400 次陀螺仪输出。
4. 求平均得到零偏，后续角速度都减去该零偏。

决定：**v1.1.0-dev 已采用**。

实施要求：

- 放入状态机的 `IMU_WARMUP` 和 `IMU_CALIBRATING` 状态，不用一整段阻塞延时。
- 校准期间电机必须为零，并用 LED 表示校准尚未结束。
- 若 400 个样本的离散程度过大，判定车辆发生移动，校准失败而不是保存错误零偏。
- v1.1.0-dev 等待 2 秒后以 5 ms 节拍采集 400 个样本，并检查样本离散范围；
  范围超过 5 dps 时拒绝保存本次零偏。
- 零偏先存 RAM。是否写入 Flash 必须单独设计磨损控制和版本校验，不能每次启动都写 Flash。

### 2.2 360°比例系数标定

原建议：

1. 原地慢转一整圈。
2. 记录程序积分显示的角度。
3. 使用 `比例系数 = 360° / 显示角度`。
4. 后续角速度或积分角度乘以该比例系数。

决定：**计划采用，但需要改成可重复标定**。

实施要求：

- 车辆必须原地、匀速、慢速旋转，避免轮胎打滑和突然启停造成的积分误差。
- 至少各做一次顺时针和逆时针 360°，最好各做三次并取平均。
- 顺时针、逆时针结果差异过大时，不能直接使用比例系数，应先检查零偏、时间步长和机械打滑。
- 比例系数作为可调参数保存；在实测完成前默认使用 `1.0`。
- 直角转弯判断使用“校正后的相对角度”，不能直接使用未经标定的 DMP 显示值。

### 2.3 静止状态下的 yaw 回零

原建议：

- 静止超过 0.5 秒；
- 当前角度在 ±6° 内；
- 每毫秒让 yaw 衰减 0.1%，缓慢拉回 0°。

决定：**待验证，不能原样无条件采用**。

原因：

- 每毫秒乘以 `0.999`，约 0.693 秒就会衰减到一半，1 秒后只剩约 36.8%，实际并不算很慢。
- 若车辆只是电机命令为零但仍在滑动或被外力推动，强制回零会制造错误角度。
- “回到 0°”只适合相对于当前直线段建立的局部航向，不适合整场累计绝对航向。

准备采用的条件：

- 电机目标速度为零；
- 陀螺仪角速度连续 0.5 秒处于小阈值内；
- 加速度模长接近 1 g 且变化较小；
- 灰度状态没有显示车辆正在穿越弯道；
- 仅修正当前局部航向参考或 gyro bias，不修改已经确认的转弯累计结果。

衰减系数必须根据真实控制周期换算，不能把“每毫秒 0.1%”直接放进 15 ms 或 20 ms 的循环里。

### 2.4 DLPF 带宽

原建议：使用 42 Hz，不使用 5 Hz。5 Hz 延迟较大，可能使快速转动的角速度波形被压低，从而导致积分角度偏小。

决定：**v1.1.0-dev 以 42 Hz 作为第一组实车参数**。

官方 MPU-6000/MPU-6050 寄存器表中，`DLPF_CFG=3` 对应陀螺仪约 42 Hz 带宽。当前工程虽然初始化过程曾调用 42 Hz，但随后 `mpu_set_sample_rate(50)` 会自动把低通设置为采样率的一半并最终选到 20 Hz。因此，v1.1.0-dev 在设置采样率之后再次明确设置 42 Hz，并通过 `mpu_get_lpf()` 回验。

不能只根据“角度大或小”判断带宽优劣。需要比较：

- 静止角速度标准差；
- 360°积分误差；
- 直角转弯峰值是否被削弱；
- 转弯结束后的振铃和噪声；
- 控制响应是否变得更抖。

## 3. 卡尔曼滤波：文章结论与本车使用方式

参考文章：

- [一文理清卡尔曼滤波，从传感器数据融合开始谈起](https://zhuanlan.zhihu.com/p/158737818)
- [TDK/InvenSense MPU-6000/MPU-6050 Register Map](https://invensense.tdk.com/wp-content/uploads/2015/02/MPU-6000-Register-Map1.pdf)
- [TI MSPM0G3507 数据手册](https://www.ti.com/document-viewer/mspm0g3507/datasheet)

### 3.1 文章的核心结论

卡尔曼滤波不是普通的“平滑曲线”算法。它循环完成两件事：

1. 根据上一时刻状态和运动模型进行预测，同时增加模型不确定度。
2. 使用新的传感器观测修正预测；预测越不可靠，就越相信观测，观测越吵，就越相信模型。

典型更新关系：

- 状态预测：`x_pred = F * x_prev + B * u`
- 协方差预测：`P_pred = F * P_prev * F^T + Q`
- 卡尔曼增益：`K = P_pred * H^T / (H * P_pred * H^T + R)`
- 状态修正：`x = x_pred + K * (z - H * x_pred)`
- 协方差修正：`P = (I - K * H) * P_pred`

其中 `Q` 表示运动模型的不确定度，`R` 表示观测噪声。卡尔曼增益不是固定常数，而是由当前不确定度决定。

### 3.2 对 MPU6050 yaw 的关键限制

**已确认：仅靠 MPU6050 的陀螺仪和加速度计，不能获得长期无漂移的绝对 yaw。**

原因是加速度计可以利用重力修正 pitch 和 roll，但绕竖直轴旋转不会改变重力方向，因此无法给 yaw 提供独立的绝对观测。卡尔曼滤波不能凭空创造一个不存在的航向参考。

因此，本车不能把“加速度计角度”直接当作 yaw 观测。可用的额外观测是：

- 静止时的零角速度约束，用来修正陀螺仪 bias；
- 车辆位于直线且灰度线稳定居中时，将局部航向误差缓慢拉向零；
- 已知直角弯道的几何约束，作为 90° 左转或右转的弱参考；
- 将来若增加编码器、磁力计或其他绝对方向来源，再做更完整的数据融合。

### 3.3 v1.1.0-dev 使用的最小方案

本版不直接上大型矩阵卡尔曼，而采用适合本车的分层处理：

1. MPU6050 硬件 DLPF 先试 42 Hz。
2. 上电等待 2 秒并用 400 个静止样本估计 gyro-Z bias。
3. 原始角速度减去 bias。
4. 对突发跳变保留当前“三点中值”拒绝机制。
5. 使用 DMP 相对 yaw，并乘以可配置的 360°比例系数（默认 1.0）。
6. 静止时使用零角速度观测缓慢更新 bias。
7. 直线且灰度稳定居中时，只缓慢修正“当前直线段局部航向误差”，不篡改直角转弯累计角度。

第二阶段再测试一个二状态滤波器：

- 状态：`局部 yaw 误差`、`gyro-Z bias`；
- 预测输入：校正后的 gyro-Z 角速度；
- 静止观测：真实角速度约等于零；
- 直线观测：局部 yaw 误差趋近零；
- 弯道和丢线状态：停止使用直线观测，只保留陀螺仪预测。

本版另外对 gyro-Z 角速度加入一维标量 Kalman 更新；它只降低角速度噪声，
不宣称能消除 yaw 漂移。完整的 yaw/bias 二状态融合仍需实车数据后再做。

## 4. 关于 MSPM0G3507 浮点运算

外部建议称：“M0+ 没有硬件 FPU，浮点只能放在中断里，主循环必须纯整数，否则浮点库状态冲突甚至死机。”

结论：前半句正确，后半句 **不要采用**。

- MSPM0G3507 使用 Cortex-M0+，没有硬件 FPU。
- 当前工程使用 `-mfloat-abi=soft`，浮点由编译器的软件库完成。
- 基本浮点运算可以在主循环执行，不会因为“主循环用了 float”自然发生库状态冲突。
- 中断服务程序应尽量短，反而不应把卡尔曼矩阵、除法、打印或阻塞 I2C 放在中断里。
- 真正需要避免的是：主循环和中断同时无保护地修改同一组多字节状态，或者在中断中调用非可重入库函数。

本项目准备采用：

- 定时器中断只增加 tick、置位采样标志或采集最小必要数据；
- 传感器读取、滤波、状态机和电机计算放在固定周期的主控制任务中；
- 性能不足时，将角度、角速度、比例系数和卡尔曼参数改成 Q 格式定点整数；
- 不为了“避免冲突”而把浮点运算搬进中断。

## 5. 状态机设计记录

### 5.1 整车状态

建议状态：

1. `BOOT_INIT`：GPIO、I2C、电机、蜂鸣器初始化，电机强制为零。
2. `IMU_WARMUP`：等待 2 秒自热，不阻塞按键和紧急停止检测。
3. `IMU_CALIBRATING`：采集 400 个静止样本。
4. `READY`：等待 K1。
5. `RUNNING`：执行循迹子状态机。
6. `PAUSED`：电机为零，保留运行参数。
7. `FAULT`：电机为零，保存具体故障码。

### 5.2 按键建议

- K1/PA2：短按在 `READY → RUNNING → PAUSED → RUNNING` 之间切换。
- K1 长按：仅在 `READY` 或 `PAUSED` 状态重新进行 MPU 校准。
- K2/PB19：降低速度档位。
- K3/PB20：提高速度档位。
- K4/PA23：任何状态下紧急停止并进入安全状态。

所有按键均为低电平有效，使用约 20–30 ms 消抖，并采用“按下边沿只触发一次”；长按和短按必须等松手后再确定，避免一次按键产生两个事件。

### 5.3 RUNNING 子状态

1. `TRACK`：正常灰度 PID。
2. `CORNER_APPROACH`：检测到直角后向前补偿车身长度。
3. `CORNER_ROTATE`：根据陀螺仪相对角度原地旋转。
4. `REACQUIRE`：新黑线连续稳定出现若干周期后才确认。
5. `LINE_LOST`：按上次方向有限时间搜索。
6. `FINISHED`：未来题目终点状态。

状态进入动作只执行一次。例如进入 `RUNNING` 时清空 PID 历史；进入 `PAUSED` 或 `FAULT` 时立即发送零速；进入 `CORNER_ROTATE` 时保存起始 yaw 和时间。蜂鸣器也只在进入状态时触发，不能在主循环中反复阻塞鸣叫。

## 6. 已发生错误与永久检查项

### 6.1 蜂鸣器

- **错误**：只配置 PWM，没有调用 `DL_Timer_startCounter(BUZZER_INST)`，结果完全不响。
- **错误**：`timerCount=99` 时把比较值设为 100，输出接近恒定电平，无源蜂鸣器只在通断时“咔哒”。
- **已验证修正**：启动 TIMA0，PB24 使用比较值约 30，可得到约 3.1 kHz 的可听 PWM。
- 修改蜂鸣器后必须先做一次“上电短响”，确认真实硬件输出，再把它用于故障编码。

### 6.2 LED

- **错误**：曾把蓝绿 LED 极性理解反。
- **已验证**：本核心板 PB2 蓝灯、PB3 绿灯均为高电平点亮、低电平熄灭。
- 不能仅凭其他 LaunchPad 示例推断定制板 LED 极性。

### 6.3 按键

- 不能猜扩展板 K1–K4 引脚；已经通过独立扫描程序实测为 PA2、PB19、PB20、PA23。
- 按键程序必须有上拉、低电平有效、消抖、边沿触发和松手确认。
- 按键扫描测试程序必须独立于电机程序，避免测试按键时意外启动车轮。

### 6.4 电机与差速

- **错误**：曾把左右差速写成前后差速，导致左转和右转时前后轮表现异常。
- 每次改变通道映射后，先架空车轮验证 M1–M4，再放到地面。
- 电机顺序、前进符号和左右分组必须使用本文件第 1.2 节的实测表。

### 6.5 DAPLink/SWD

- PA19 是 SWDIO，PA20 是 SWCLK，不能接反。
- DAPLink 的 3V3 通常是目标参考/小电流供电，不能给整套电机系统供电。
- `Error connecting DP: cannot read IDR` 首先检查 SWDIO、SWCLK、GND、RST、目标 3.3 V 和是否存在并行供电；不能直接认定芯片被程序烧坏。
- 普通应用程序烧录只写主 Flash，不修改 NONMAIN、BSL 或安全配置。
- 除非明确进行芯片恢复，否则不要修改 NONMAIN、启动安全区、SWD策略或执行全片不可逆保护。

### 6.6 供电与倒灌

- 车辆接电池运行时，避免同时通过核心板 Type-C、电机驱动 Type-C、扩展板 OUT/I2C 5V 等多个路径供电。
- 只连接调试信号不代表没有供电路径；5V、3V3、I2C线上的保护结构都可能形成反向供电。
- 接线变化后先断电，用万用表直流档检查电源轨和地，再上电。
- 电机测试前架空车轮，确认急停按钮可用。

### 6.7 SysConfig、编译与源码

- `.syscfg` 是引脚和外设配置的源文件。
- 不手工修改 `build/`、`Debug/` 内生成的 `ti_msp_dl_config.c/.h`、链接文件或 `.out`。
- 生成成功、编译成功、烧录成功、实物工作正常是四个不同结论，报告时必须分开。
- 不得把未经过编译的临时版本交给用户烧录。
- 不得未经用户要求直接烧录；默认只修改、生成、编译并给出烧录命令。
- 修改前保留上一版项目；未经用户明确要求不推送 Git。

### 6.8 控制周期和文档一致性

- 当前源码中控制循环名义延时为 15 ms，但 I2C、灰度采样和 DMP读取还会增加执行时间，真实周期不是精确 15 ms。
- 当前 README 曾写“约 20 ms”；后续必须通过定时器或 GPIO/逻辑分析仪测得真实周期。
- 积分角度、PID 的 I/D 项、静止 0.5 秒判断和 yaw 衰减都必须使用同一个真实 `dt`，不能一部分按 15 ms、一部分按 20 ms。
- 每次改采样率、DLPF 或控制周期，都要同步更新本文档和 README。

## 7. v1.2.0-dev 实施记录与下一步

1. 已完成：加入四按键 GPIO、消抖和边沿事件。
2. 已完成：实现 `IMU_WARMUP → IMU_CALIBRATING → READY` 非阻塞流程。
3. 已完成：实现 K1 启停、K2/K3 速度调节和 K4 紧急停止。
4. 已完成：零偏改为 2 秒预热后 400 样本，并拒绝明显移动的校准。
5. 已完成：DLPF 明确设为 42 Hz 并回读验证。
6. 已完成：加入一维角速度 Kalman、静止 bias 慢速修正和 yaw 比例系数接口。
7. 已完成：直角弯阶段保留 150°安全上限，但对超过 45°的单次 DMP 跳变
   直接重建 wrapped-yaw 基准，避免异常包导致后续永久连续拒绝。
8. 已完成：直角弯同时积分短时 gyro-Z 角度，DMP 丢失角度时使用两者中
   符合转向方向且进度较大的结果。
9. 已完成：直角弯固定使用 400/340 转速，丢线搜索使用 360，不随低速
   巡线档位降低，并将丢线超时从 40 周期延长到 80 周期。
10. 已完成：只有中间两路同时检测到黑线才确认转弯重新捕线和直线居中。
11. 已完成：单中间传感器偏差加快积分；回到双中间传感器后保留学习到的
   电机机械偏差补偿，不再每周期快速衰减。
12. **实车失败记录**：400/340 的直角旋转速度过高，车辆可能在中央探头
    捕获条件连续满足前直接越过新黑线，随后持续同方向旋转。
13. **实车失败记录**：145°角度上限过宽；如果方向符号或积分进度异常，
    原有保护无法在物理旋转约 90°时及时刹停。
14. **结论**：`v1.2.0-dev` 只用于保留问题现场，不能作为稳定比赛版本。
15. 下一版必须降低后半段转速，在接近 90°时主动刹车，并使用与正负方向
    无关的绝对角度和更短时间双重保险，禁止车辆持续转圈。
16. 待验证：实测顺/逆时针 360°，再把比例系数从 1.0 改为实测值。
17. 后续：有可靠数据后再做局部 yaw/bias 二状态融合。

每一步单独编译、单独实测、单独记录结果，避免一次更改多个变量。
